export class AbsSetting {
    customerAbsEnabled: boolean;
    salesAbsEnabled: boolean;

    constructor(customerAbsEnabled: boolean, salesAbsEnabled: boolean) {
        this.customerAbsEnabled = customerAbsEnabled;
        this.salesAbsEnabled = salesAbsEnabled;
    }
}
