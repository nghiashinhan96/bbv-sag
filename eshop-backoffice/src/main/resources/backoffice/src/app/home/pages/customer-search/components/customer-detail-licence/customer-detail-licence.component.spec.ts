/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CustomerDetailLicenceComponent } from './customer-detail-licence.component';


xdescribe('CustomerDetailLicenceComponent', () => {
  let component: CustomerDetailLicenceComponent;
  let fixture: ComponentFixture<CustomerDetailLicenceComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CustomerDetailLicenceComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CustomerDetailLicenceComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
*/
