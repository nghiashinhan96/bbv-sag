/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CustomerDetailSettingComponent } from './customer-detail-setting.component';


xdescribe('CustomerDetailSettingComponent', () => {
  let component: CustomerDetailSettingComponent;
  let fixture: ComponentFixture<CustomerDetailSettingComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CustomerDetailSettingComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CustomerDetailSettingComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
*/
