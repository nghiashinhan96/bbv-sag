package com.sagag.services.copydb.repo.src;

import com.sagag.services.copydb.domain.src.EshopFunction;

import org.springframework.data.jpa.repository.JpaRepository;

public interface EshopFunctionRepository extends JpaRepository<EshopFunction, Integer> {
}
