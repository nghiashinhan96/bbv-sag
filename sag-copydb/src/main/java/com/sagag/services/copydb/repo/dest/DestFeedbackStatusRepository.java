package com.sagag.services.copydb.repo.dest;

import com.sagag.services.copydb.domain.dest.DestFeedbackStatus;

import org.springframework.data.jpa.repository.JpaRepository;

public interface DestFeedbackStatusRepository extends JpaRepository<DestFeedbackStatus, Integer> {
}
