package com.sagag.services.copydb.repo.dest;

import com.sagag.services.copydb.domain.dest.DestRolePermission;

import org.springframework.data.jpa.repository.JpaRepository;

public interface DestRolePermissionRepository extends JpaRepository<DestRolePermission, Integer> {
}
