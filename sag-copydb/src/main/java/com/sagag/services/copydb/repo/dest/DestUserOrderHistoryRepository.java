package com.sagag.services.copydb.repo.dest;

import com.sagag.services.copydb.domain.dest.DestUserOrderHistory;

import org.springframework.data.jpa.repository.JpaRepository;

public interface DestUserOrderHistoryRepository extends JpaRepository<DestUserOrderHistory, Long> {
}
