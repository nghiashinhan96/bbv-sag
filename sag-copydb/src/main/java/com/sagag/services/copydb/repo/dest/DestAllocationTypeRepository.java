package com.sagag.services.copydb.repo.dest;

import com.sagag.services.copydb.domain.dest.DestAllocationType;

import org.springframework.data.jpa.repository.JpaRepository;

public interface DestAllocationTypeRepository extends JpaRepository<DestAllocationType, Integer> {
}
