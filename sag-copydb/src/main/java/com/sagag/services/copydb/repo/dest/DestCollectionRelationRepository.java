package com.sagag.services.copydb.repo.dest;

import com.sagag.services.copydb.domain.dest.DestCollectionRelation;

import org.springframework.data.jpa.repository.JpaRepository;

public interface DestCollectionRelationRepository extends JpaRepository<DestCollectionRelation, Integer> {
}
