package com.sagag.services.copydb.repo.dest;

import com.sagag.services.copydb.domain.dest.DestExternalOrganisation;

import org.springframework.data.jpa.repository.JpaRepository;

public interface DestExternalOrganisationRepository extends JpaRepository<DestExternalOrganisation, Long> {
}
